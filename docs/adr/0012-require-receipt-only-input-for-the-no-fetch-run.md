# Require receipt-only input for the no-fetch run

The genuine report-only live manual prediction must select the R3 receipt odds source and fail closed unless a fresh, verified, sealed collector receipt already exists. Automatic and synchronous capture are prohibited for this acceptance run. Evidence produced independently before the prediction may be consumed when it satisfies the freshness and provenance gates.
