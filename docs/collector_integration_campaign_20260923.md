# Collector engineering campaign

The user's 23 September engineering authorization governs this work. Preserve all
prior attempts; maximum 12 new future race/window attempts, 48,000 guarded logical
source requests, and 10,800 seconds of live operation across all launches. No
prediction, research, protected history, model or retention activation. Permanent
rollout and merge require final approval.

Acceptance: exercise generated commands with pinned interpreter, actual capture
child, canonical identity, strict original reservation/window, native append,
receipt consumer, monitor and restoration against fabricated transport first.
Include expiry, delayed startup, missing startup evidence, interruption, and shared
consumption on restart/across lanes. Then seek 90 continuous minutes on unchanged
code, both lanes progressing, at least three distinct valid captures if available,
multiple window types where feasible. Record startup unavailability separately,
index age target 270 seconds (R3 rejection remains 300), candidate native readiness,
all observed eligible/rejected/missed/pending opportunities and lane/request totals.
Finite sparse success does not establish burst capacity or installed R3 binding.

Window contract: planner admission starts at jump minus offset. The next smaller
offset closes the original window exclusively; T-2 closes at jump. Receipt source
stamp tolerance opens 180 seconds earlier but does not authorize early planning.
The named profile binds the same window at subprocess planning, fetch and append.
The latest BEN R7 reservation was consumed at 16:11:00.927768 after its T-10 close
of 16:11:00; subprocess planning at 16:11:01.319266 rejected the changed window.
Prior consumption is immutable. New stale work is excluded before consumption;
a reserved attempt remains spent if startup/fetch/append expires. Admission funds
the existing 50-second phase before reserving; this does not extend window expiry.

Campaign accounting is outside launch directories. Each launch reserves the full
live/cleanup lease; only verified restoration replaces it with elapsed time.
Unclosed launches retain the full conservative charge and prevent another owner.
Requests and attempts use a durable mutex-protected ledger; browser navigations
are also charged, separately from observed browser subresources and wire retries.
The campaign restoration allowance is 31 minutes: an interrupted T-60 capture can
leave nearly 30 minutes before the next window, and the installed legacy collector
has no durable reservation guard. Waiting for closure before restarting legacy
triggers preserves consumed windows. Waiting is conservatively charged as live time.

Live stop: integrity failure, source denial, protected-data exposure, accounting
loss, phase/timing failure or bounds exhaustion. Restore exact paired units,
enablement/activity and unchanged R3 process. Correct demonstrated failures offline
before further live use; no unchanged-code repetition for a lucky pass.
