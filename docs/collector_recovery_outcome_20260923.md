# Collector recovery continuation — 23 September 2026

**Metadata access recovered; the collection goal is incomplete and permanent
rollout remains HOLD.** This is the same campaign and ledger, not new allowances.

At 19:55:48 AEST the exact packaged production NextEvents client made one logical
GET using the existing route and identity. HTTP 200 returned 240 events and passed
the additional current-greyhound identity/time/track-data checks. The sanitized
snapshot hash, status, timestamp and state transition are retained in
`access-review-20260923/metadata-recovery-result.json` under the campaign root.
No raw payload, odds, result or prediction was printed or stored by that diagnostic.
The single recovery allowance is now 1/1; success did not reset it. A renewed
denial must stop shared source activity with no second recovery.

The [access review](collector_sportsbet_access_review_20260923.md) resolves the
prior indefinite evidence hold by a scoped interpretation of current terms; it
is not a provider-issued licence. Its exact references and reasoning were copied
to immutable campaign evidence and adopted under the existing state lock. The
original denial and deadline remain. The [operating policy](collector_sportsbet_operating_policy_20260923.md)
bounds the finite trial; numerical limits are local controls, not claimed safe
or provider-approved rates. Its evidence clarification preserves the initial note
and corrects unsupported per-batch arithmetic without changing any limits.

## What is and is not proved

Recovery previously reopened after HTTP 200 even for challenge HTML, wrong JSON,
empty metadata and unvalidated browser data. Four offline regressions reproduced
that failure. Recovery now requires usable route data inside its owned operation.
Browser acceptance uses the existing race/runner/WIN-and-PLACE validator before
closing the browser. Original validation, append-time windows, append and receipt
checks remain unchanged.

32 focused source/metadata checks passed. Three changed packaged cases passed
with the exact campaign interpreter in 94.28 seconds: valid browser recovery
with real synthetic appends/receipts and both entrypoints; wrong-race recovery
with no append and durable STOP; Python denial with both entrypoints blocked.
Fabricated transport and kernel network denial remain explicit limitations.
Standards review: zero findings. Spec review: zero findings, both against
`234415e1...6e7fa6e4`. The later fixture-only change propagates a controlled
wall-clock offset into offline child processes; no production clock was changed.

Retained failures: ordinary pytest conftest could not import flask_compress;
focused tests use the established `--noconftest` mode, without installing packages.
The first packaged run was rejected by the real same-date cutoff. The initial
fixture clock did not reach child service environments and correctly failed
`operating_scope_closed`; propagating it only in the offline fixture fixed that.
Neither production operating limits nor reservation times were relaxed.

The live diagnostic exercised metadata transport and its validator only. It did
not install the paired candidate, reserve a race/window, start a capture browser,
append live odds, publish an index or receipt, or establish sustained collection.
The installed index source timestamp was still 17:25:11.838527 AEST: its age at
19:59:37 was 9,265.88 seconds, far beyond R3's unchanged 300-second limit. Running
R3 therefore does not establish fresh usable inputs.

The previous observed eight eligible windows remain one failed/consumed, two
missed and five pending at the earlier stop. No opportunity inventory was taken
for the later paused interval; missed-window counts are unknown, not zero. Startup
unavailability, native readiness, lock waits and sustained resource consumption
on the new candidate remain unmeasured. Prior timings and fixture timings cannot
fill those gaps.

The planner still uses T-60/T-30/T-10/T-2 and exclusive next-window boundaries.
The native receipt consumer verifies exact collector artifacts, identities and
freshness; a fresh current index is not complete window coverage. R3's additional
journal prerequisite remains separate from collector freshness and this campaign
does not generate its prediction precursors or change those admission checks.

## Source traffic and current state

The old 404 logical calls comprise 15 Sportsbet Python, 372 TheDogs Python,
15 weather Python and two browser navigations. The retained browser observer saw
64 Sportsbet request events and 128 other-host events; those overlap navigations
and must not be summed as additional logical requests. Ordinary legacy traffic,
earlier hidden retries and complete wire activity are absent. The new recovery
adds exactly one Sportsbet Python call. No browser traffic was generated now.

Cumulative consumption: **1/12 capture attempts, 405/48,000 logical requests,
721.058807/10,800 charged seconds**. Remaining: 11 attempts, 47,595 logical requests,
10,078.941193 seconds. The metadata operation's 0.158876-second charged lease is
closed. All earlier evidence, failed attempts and denial history remain retained.

Both collector timers remain inactive/disabled, services inactive with MainPID 0.
The intentional collection interruption that started at 17:39:21 AEST remains
open. Original unit hashes and R3 PID 149626/binding are unchanged. The real shared
collector lock is absent; there is no unclosed campaign lease or armed future
launch. The guarded candidate units have not been installed. The source gate is
OPEN under the scoped access basis and finite policy, with recovery consumed 1/1.

## Exact candidate, next operation and permanent decision

Reviewed execution commit: `6e7fa6e423655df22a6769e524b18c975359b2f6`.
Tree: `76875494de5f166270eec91fb135eb69b9f2a7c6`.
Package: `/home/l4nd0/greyhound-collector-campaign-20260923/access-review-20260923/release-candidate-6e7fa6e4`.
Archive SHA256: `129d404b57334ad63cdd5b00d60e43236fab9dfcf054689c136b9ba1c779e39a`.
Plan digest: `73fa3c32d437013b70de7ce13fd775d33a446e3aa2c370b81e2decfb10045d64`.
Its 793 exported execution/config files equal the successful packaged fixtures.
`ROLLOUT_DECISION.json` records the pinned interpreter/runtime, exact paired unit
hashes, installed R3 binding delta and rollback references.

A 90-minute observation plus its existing 31-minute cleanup must end by 21:20 AEST;
latest same-day start was 19:19. That admission window had already closed during
this continuation. No backdated start, shorter acceptance run, altered cutoff or
unattended future launch substitutes for it. A package is prepared for **24 September,
12:00–13:30 AEST**, with cleanup ending 14:01, but nothing is scheduled or armed.
The existing campaign authorization is sufficient to resume that supervised
operation after live state/budget/quiescence checks; no routine approval is needed.
Any naturally eligible opportunities must be observed then, without reusing
consumed windows. Expired package windows require fresh packaging, not relabeling.

Permanent rollout decision: **HOLD**. Do not merge PR #184, permanently install
collector units, or change R3. A completed unchanged 90-minute candidate with
both-lane progress and native valid captures is still required. After that,
prepare stable production paths and reconcile R3's source/unit/deployment authority
against the exact pair; candidate-native readers cannot prove installed R3 readiness.

Conditional paired rollback: stop both triggers, keep legacy enablement disabled,
wait for natural service drain and genuine shared-lock release. Verify installed
unit hashes still belong to the known candidate or baseline before replacing
both pairs with their exact retained original bytes and reloading systemd.
Unknown intervening unit changes require reconciliation, not overwrite. Keep
triggers inactive because the restored legacy source lacks shared coordination.
Never roll back the database, ledger, reservations, published records, receipts or
source-access state. R3 unit/binding references are retained for a future separately
approved rollout; this continuation did not modify or restart R3.

The source access decision no longer requires an external letter. The optional
unsent provider inquiry in the access review remains available for long-term
contractual/capacity clarification; it is not an outstanding approval prerequisite
for the authorized bounded trial. The task remains incomplete because live capture
and the required sustained observation have not been demonstrated.
