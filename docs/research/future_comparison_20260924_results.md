# Frozen future comparison — execution report

The four-way retained-input comparison is implemented, default off, with two
separate fitted experimental artifacts. Production is unchanged. See the
[handoff](future_comparison_20260924_handoff.md) and the
[unactivated execution specification](future_comparison_20260924_spec.md).

Training used only the qualified 331-race / 2,360-runner development population
June 10–July 8, with exact 2,360-row raw-card feature reconstruction. No feature
search or new backtest was run. The 177 already inspected evaluation races were
used only for the explicitly exploratory variance planning calculation. Initial
fit and one source-provenance replay after removing unused imports/whitespace
used identical coefficients/preprocessing; each pair of fits took about one CPU
second. The original registry is preserved in the isolated output directory.
Final model identities are recorded in the handoff and candidate registry.

The old [access incident](offline_20260924_access_incident.json) is copied here
byte-for-byte from the prior research worktree because that file was ignored by
Git in #193. It is not rewritten or retracted. Protected label exposure and its
separate timing-metadata design influence remain part of the historical record.
No actual protected or future target outcomes were accessed in this task.

Pre-package verification: 96 targeted synthetic/default-off/retained-worker
checks passed in 40.50 seconds under kernel networking denial, one thread, nice
10. A shared-temporary-directory run previously produced two transient receipt
protocol failures; its log is preserved. The collector owner independently
reproduced sibling-publication invalidation and fixed it in `a15f5882`; that
committed fix will be integrated before the final exported-source proof.

Exported-package measurements and exact integration commit will be appended
following execution; these pending measurements are not future-race evidence.
All working evidence is isolated under
`/home/l4nd0/greyhound-future-comparison-output-20260924/`.
