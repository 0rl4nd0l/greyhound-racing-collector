# Validation

result: WORKING

## Control plane and hooks

- Merged Tenn canonical: `af1b33eb2a5e203b21338eaa0a7e1de95362ed58`.
- `$HOME/.codex/skills/tenn-git-guard` resolves to that stable canonical
  checkout.
- Installed and canonical portable runners are byte-identical at SHA-256
  `f30deae04fc7c03da922dcb800618009cc5f1b387c98d79a286dcce65c749784`.
- Hook JSON parses and requires `TENN_V2_REQUIRED=1` for both `BeforeTool` and
  `Stop`, through the `$HOME/.codex` runner and a Git-derived repository root.

## Focused behavior

- Focused Greyhound V2 suite: 22 passed.
- Missing-control-plane portability check: the repo-local hook-shape test still
  passed; ten cross-repository integration cases skipped explicitly.
- Exact recorded 663-race scope: `REUSED_COMPLETE`, exit 3, report writes
  denied, and no report directory created.
- Changed evidence/dataset and a genuinely new hypothesis: admitted.
- Third related unchanged no-delta continuation: `LOOP_GUARD_STOP`, with no
  report.
- Prospective `DATA_MISSING`: blocked its exact comparison while the declared
  offline `RESEARCH_FIT` transition remained admitted.
- Empty-delta `ADVANCED`, terminal `NEXT_GOAL.md`, no-card `systemctl`, and
  report-only research fitting: rejected without a decision append.
- Linked worktrees resolved the same Git-common-dir decision ledger.

## Seed and first-five evidence

- Deterministic seed regeneration with `--check`: four entries, unchanged.
- Tracked seed SHA-256:
  `5a4f37205845d5ebecd3f08d1c0f1bba15b1f90fddaf8c472835563a9a2a8727`.
- Pre-release shared ledger: 14 valid rows, including each approved seed ID
  exactly once; the manifest is byte-identical to ledger rows 2 through 5.
- First five distinct post-pilot fingerprints:
  `6dfef6538738`, `d7205b754f0e`, `d43f6fb76489`, `3805d972a3d9`, and
  `62b000e01c68`; all have durable `ADVANCED` outcomes and zero false blocks.
- Two legacy duplicate chains remain append-only evidence and were not edited.

## Static and contract checks

- Ruff over both focused test files: passed.
- Python compilation over both focused test files: passed.
- Independent skeptical review: no critical findings, warnings, or
  suggestions.
- `git diff --check`: passed.
- V2 task-card validation, allowed-file diff, candidate validation, and
  closeout validation: passed.

No model, database, timer, service, registry pointer, or production runtime
mutation was performed.
