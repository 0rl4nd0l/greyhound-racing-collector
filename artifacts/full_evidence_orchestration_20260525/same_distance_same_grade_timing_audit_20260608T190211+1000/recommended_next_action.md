# Recommended Next Action

Chosen action: keep `quarantine_feature` policy.

Do not implement a bounded population repair for `same_distance_same_grade_best_time` or `same_distance_same_grade_avg_time` yet. In the active shadow training split, strict safe recoverability is `0/751` missing rows.

Next data work should improve safe target metadata ingestion first, then collect or verify more canonical official prior history. The repair can be reconsidered only after target distance/grade coverage and prior same-distance/same-grade individual-time coverage are present in training rows. Shadow mode can continue with the current quarantine policy; this remains a blocker only for claiming the full 78-feature schema is fully learned end to end.
